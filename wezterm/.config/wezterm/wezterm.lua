local wezterm = require("wezterm")

return {
  font = wezterm.font('FiraCode Nerd Font Mono'),
  color_scheme = "ayu",
  enable_tab_bar = false,
  window_padding = {
    left = 20,
    right = 20,
    top = 20,
    bottom = 10
  }
}
